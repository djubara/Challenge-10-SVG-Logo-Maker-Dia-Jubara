// Used to validate the user input for the logo characters. The function should return true if the user input is valid and false if the user input is invalid. The function should also log an error message if the user input is invalid. The function should be exported and used in the index.js file.
validate(logoChars, charsColor, logoShape, logoBgColor) => {
    if (answer.length === 0 | answer.length > 3) {
        return console.log("\n Please enter 1 to 3 characters");
    }
    return true;
}